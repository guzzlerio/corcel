image: golang:1.5.1

before_script:
    - cp -r ../../guzzler $GOPATH/src/ci.guzzler.io
    - cd $GOPATH/src/ci.guzzler.io/guzzler/corcel

stages:
    - build

job1:
    stage: build
    script:
        - make install
        - make build
        - make test